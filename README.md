To get started:

```bash
git clone https://github.com/wathika-eng/kata-machine
cd kata-machine

# to get latest yarn version 4.9.3
yarn set version stable

# to install dependencies
yarn

# to generate the initial project structure
# not compulsory
#yarn generate

# to test 
npx jest <name_of_algorithm>
npx jest linear
```
